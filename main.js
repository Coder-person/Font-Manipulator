function preload(){

}

function setup(){
    canvas = createCanvas(400,400);
    canvas.position(700,100);
    video = createCapture(VIDEO);
    video.size(400,400);
}

function draw(){
    
}